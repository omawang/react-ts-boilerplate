import { makeStyles, Theme } from '@material-ui/core';

export const LogOnLayoutStyles = makeStyles((theme: Theme) => ({
  root: {
    width: '100%',
    height: '100vh',
    overflow: 'hidden',
  },
}));
